# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Build

Requires `clang++` and **CMake ≥ 3.20** with a generator (Ninja preferred, Unix Makefiles otherwise). **CMake ([CMakeLists.txt](CMakeLists.txt)) is the single build system of record** on every platform; a thin **`Makefile` wrapper** preserves the historical `make …` UX by invoking CMake, so `make` and `cmake` are interchangeable on Unix-y shells (on Windows, invoke CMake directly). There is no code-generation build step; the precomputed bitboard lookup tables in `src/generated_data.h` are `inline const` globals computed once at program startup.

The engine is **header-only**: every engine class/function lives in `src/*.h` as `inline` definitions, so there are no per-class engine `.cpp` files. The only compiled engine translation unit is `main.cpp` (the UCI entry point); each test and tool compiles as a single TU that pulls the whole engine in through its headers and links on its own. Because `game` ↔ `search_state` ↔ `evaluation` form an include cycle, `search_state.h` is the **hub**: it defines `class SearchState` (which needs only a forward-declared `Game` for its `Game &game_` member), *then* includes `game.h` and holds the inline definitions of the `SearchState` methods and `EvaluatePosition` (which need `Game` complete). `game.h` defines `class Game`, pulls the hub in at its very bottom (after `class Game` is fully defined), and then defines `Game::SearchRootNode` there — where `SearchState` is now complete. So `#include "game.h"` alone yields a complete, searchable engine in either include order (guarded by `#pragma once`).

The `make` targets are a thin wrapper over CMake (each `make X` runs a `cmake --build`):

```bash
make          # engine → build/pawnstar_<major>_<minor>_<build> (+ the build/pawnstar alias)
make check    # build everything and run all test suites via ctest
make tools    # build helper tools (stamp_net, filter_book, nnue_quant_study, dump_magics)
make doc      # generate Doxygen HTML into doc/html
make clean    # remove the build/ tree
```

Equivalently — and as Windows builds — drive CMake directly:

```bash
cmake -S . -B build -G Ninja -DCMAKE_CXX_COMPILER=clang++   # configure
cmake --build build                                         # engine + tools + tests
ctest --test-dir build --output-on-failure                  # run the suites
```

Wrapper knobs map to CMake cache vars: `RELEASE=1`→`-DPAWNSTAR_DEBUGX=OFF`, `DEBUG=1`→`-DCMAKE_BUILD_TYPE=Debug -DPAWNSTAR_SANITIZE=ON` (ASan+UBSan), `WERROR=1`→`-DWERROR=ON`, `CXX=…`→`-DCMAKE_CXX_COMPILER=…`, `BASH=…`→`-DBASH_EXE=…` (the shell for the UCI ctest; needed on macOS, whose `/bin/bash` 3.2 lacks `coproc`). `make check` runs `ctest -V`, streaming each suite's own output (the `[PASS]`/`[FAIL]` lines, perft/bench numbers); pass `CTEST_ARGS=--output-on-failure` for a terse summary.

The engine binary is **version-named** `pawnstar_<major>_<minor>_<build>` (+ `.exe` on Windows). Major/minor are `kVersionMajor`/`kVersionMinor` in [src/version.h](src/version.h) (the single source of truth, edited by hand); the build number is the git commit count (`git rev-list --count HEAD`) — globally reproducible (identical for every clone at a given commit), 0 outside a git checkout. CMake stamps it into the engine via the `PAWNSTAR_BUILD_NUMBER` macro (a per-build [cmake/write_build_number.cmake](cmake/write_build_number.cmake) step force-includes a generated header, rewritten only when the count changes) and into the output filename; the `make` wrapper passes the count as `-DPAWNSTAR_VERSION_BUILD` so the filename tracks each commit without a manual reconfigure (direct-CMake users reconfigure to refresh the filename — the *embedded* version always tracks). `handle_uci` reports it as `id name Pawnstar <major>.<minor>.<build>` (`VersionString()`). Each build also maintains a stable **`build/pawnstar`** alias (a `POST_BUILD` symlink on Linux/macOS, a copy on Windows where symlinks need privilege) pointing at the newest version-named binary, so tooling/scripts/docs (`.vscode/launch.json`, `tools/run_sprt.sh`, `tools/rate.sh`, `test/uci_test.sh`) can use that fixed path. `make clean` removes the `build/` tree. CI checks out full history (`fetch-depth: 0`) so the count isn't undercounted, and on success uploads a per-OS version-named binary as an artifact (glob `build/pawnstar_*`, which excludes the alias).

Compiler flags include `-mbmi2` and `-mavx2` (AVX2 for the NNUE kernels; `-mbmi2` lets the compiler emit BMI1 `tzcnt`/`blsr` instructions for the `std::countr_zero` bit iteration; both present on Intel Haswell / AMD Zen 1+), `-std=c++23`, and `-I src`. Sliding-piece attacks use **magic bitboards** (a portable 64-bit multiply), not BMI2 `pext`, so no fast-`pext` CPU is assumed (BMI2's only hard requirement, `_pext_u64`, was removed with the pext path). Debug builds (`DEBUG=1` → CMake `Debug` + `PAWNSTAR_SANITIZE`) add `-fsanitize=undefined,address`. Diagnostic counters (`DEBUGX`) are compiled in by default; `RELEASE=1` (`-DPAWNSTAR_DEBUGX=OFF`) omits them. There is **no LTO**: with the whole engine header-only, each translation unit already inlines across former `.cpp` boundaries at `-O3`, so ThinLTO bought nothing and was removed (perft is on par or faster without it). Warnings are on (`-Wall -Wextra -Wpedantic`) but not fatal by default; `WERROR=1` (`-DWERROR=ON`) adds `-Werror` (for CI).

The `make` wrapper reconfigures on every invocation, so branch/knob changes are picked up automatically; if a build ever looks stale, `make clean` (removes `build/`) and rebuild. Switching `CXX` for an existing tree needs `make clean` first — CMake caches the compiler.

**Cross-platform build (Windows).** CMake is the single build system across Linux, macOS and Windows; the `Makefile` wrapper is just a Unix-shell convenience over it (it works in Git Bash too if `cmake`+`ninja` are installed, but on Windows CMake-direct is the norm). `cmake/mingw-clang-toolchain.cmake` cross-compiles a self-contained statically-linked `pawnstar.exe` from Linux (clang → `x86_64-w64-windows-gnu`), with `wine` as the ctest emulator; the Windows build is bit-identical (same `bench` node count). On Windows the **CI-validated path is CMake + Ninja with the VS-bundled clang** (targets the MSVC ABI/STL, `x86_64-pc-windows-msvc`): `cmake -S . -B build -G Ninja -DCMAKE_CXX_COMPILER=clang++`. `CMakeLists.txt` defines `_CRT_SECURE_NO_WARNINGS` on `WIN32` (silencing MSVC's UCRT `getenv`/`setbuf` deprecation warnings) so `-DWERROR=ON` stays clean. **MSVC (`cl.exe`) is not a wired-up build path, but the engine *source* is `cl`-compatible**: under `cl /std:c++latest /arch:AVX2 /EHsc /Zc:preprocessor` the whole engine compiles, links and runs **bit-identically** (same `bench` node count, measured) — the `[[gnu::target(...)]]` attributes are harmlessly ignored (the VNNI `vpdpbusd` kernel stays runtime-gated by `kCpuHasAvxVnni`, and `_mm256_dpbusd_avx_epi32` is available under `/arch:AVX2`), and there are no GCC builtins or inline asm in the tree. The one real gap is embedding the net/book: the `.incbin` `.S` files (below) can't be assembled by `cl`, so a pure-`cl` build needs a different embed mechanism (e.g. a generated byte-array `.cpp`). Clang stays primary anyway: it's CI-validated and ~5–15% faster nps (MSVC's SIMD codegen trails it). The only OS-specific code is `src/platform.h` (CPU-feature cpuid: `<cpuid.h>` vs `<intrin.h>`) and `main.cpp`'s `ExecutablePath` (`/proc/self/exe` vs `GetModuleFileNameW`); the embedded-data `.S` files guard their ELF-only directives behind `__ELF__`. Keep new code portable: prefer C++ standard facilities (e.g. `<bit>` over `__builtin_*`) and route any new OS/compiler-specific need through `platform.h`.

## Running Tests

Tests are standalone executables in `test/`, each a single translation unit that pulls the header-only engine in through its includes and supplies its own `main()` (no engine objects to link against). Build and run all tests with:

```bash
make check    # builds tests + tools + the engine, then runs all eight suites; returns non-zero on any failure
```

Individual test executables:

```bash
./build/test_see              # 24 static exchange evaluation cases
./build/test_perft            # move generation regression (standard PERFT positions)
./build/test_bratko_kopec_nnue nnue/pawnstar-v12.bin   # 24 Bratko-Kopec positions searched with NNUE; reports moves solved
./build/test_nnue nnue/pawnstar-v12.bin test/nnue_reference.txt   # NNUE inference cross-check vs trainer
./build/test_nnue_incremental nnue/pawnstar-v12.bin              # incremental accumulator == full refresh
./build/test_opening_book     # opening-book parse/lookup regression
./build/test_chess_clock      # ChessClock std::chrono timing logic (deadline / elapsed / ponderhit / reset)
```

The eight suites are `test_perft`, `test_see`, `test_bratko_kopec_nnue`, `test_nnue`, `test_nnue_incremental`, `test_opening_book`, `test_chess_clock` (this one a self-contained timing unit test — header-only `ChessClock`, no net needed) and `test/uci_test.sh` (a shell-driven UCI integration test run against the freshly built `build/pawnstar`). `make check` builds the whole project — engine, the helper tools (`stamp_net`, `filter_book`, `nnue_quant_study`, `dump_magics`) and the test binaries — and runs the suites via `ctest`, so a broken tool or a broken engine fails the check rather than rotting unnoticed; the UCI integration test drives the freshly built engine.

`make check` runs the NNUE tests against the shipped `nnue/pawnstar-v12.bin` (the NNUE ctest cases are registered only when the shipped net is present, so a checkout without the `.bin` still configures and those checks simply don't run):
- `test_nnue` (`test/nnue_test.cpp`) takes `<net.bin> <reference.txt>` and checks two things per position: (1) the **exact int16 reference** (`Network::EvaluateExact`) matches the trainer's reference evals within ±2 cp (the checked-in `test/nnue_reference.txt` is 250 evals for `pawnstar-v12.bin`, generated by the bullet `pawnstar_eval` example — regenerate it if you ship a new net); and (2) the **shipped int8 output layer** (`Network::Evaluate`) stays within 40 cp of that reference (measured max 26 cp — the SCReLU-into-8-bits requant). With no arguments it is a no-op.
- `test_nnue_incremental` (`test/nnue_incremental_test.cpp`) takes `<net.bin>` and asserts the incrementally-maintained accumulator evaluates bit-identically to a full refresh over random move sequences (no-op without a net).
- `test_bratko_kopec_nnue` (`test/bratko_kopec_nnue_test.cpp`) takes `<net.bin> [depth]` and searches the 24 BK positions (single-threaded, deterministic) with NNUE. It is a **hard gate: it must solve all 24** — each position's best move must be in that position's accepted-move set in the `kCases` array in `test/bratko_kopec_nnue_test.cpp` (returns non-zero otherwise); no-op without a net. The accepted moves are net-specific (the union of the engine's deterministic best moves over depths 8–11), so **regenerate them when shipping a new net** — see [nnue/README.md](nnue/README.md) §7 step 7.

NNUE is the only evaluator (the handcrafted eval was removed), so the Bratko-Kopec suite is the **move-based** `test_bratko_kopec_nnue` only — there is no score-based BK test, because NNUE scores are non-deterministic under Lazy SMP and on a different scale. The 24 positions and their reference best moves live in the `kCases` array in `test/bratko_kopec_nnue_test.cpp`. The former score-based `test_bratko_kopec` and the `test_pawn_structure` suite tested the removed handcrafted evaluation and were deleted with it.

## Continuous integration

`.github/workflows/ci.yml` runs on every push to `main`, every pull request to `main`, and on manual dispatch. Three parallel `ubuntu-latest` jobs gate the branch (all pinned to clang-18 to match local): **build-test** (`make -j WERROR=1 CXX=clang++-18 check` — engine, tools and all eight suites, behind a fail-fast AVX2/BMI2 runner check), **format** (`clang-format-18 --dry-run -Werror` over `src/ test/ tools/` — the whole tree must be clang-format-clean), and **cmake-ctest** (the cross-platform CMake build + `ctest`, so the Windows build path can't rot). The README carries a CI status badge. `main` is not branch-protected, so CI reports but does not block direct pushes.

Three additional **artifact jobs** (`artifact-linux` ubuntu-latest, `artifact-macos` macos-latest/arm64, `artifact-windows` windows-latest) each build a release binary, run the full suite on that platform, and on success upload the version-named executable for download. The macOS job `brew install bash`s a modern bash (the system `/bin/bash` is 3.2 and lacks `coproc`, which `uci_test.sh` uses) and passes it via `make … BASH=…`. The Windows job uses the third-party `ilammy/msvc-dev-cmd` (MSVC STL/SDK) + `seanmiddleditch/gha-setup-ninja`.

## Sibling ports

Two **bit-exact ports** of this engine exist as sibling checkouts: **Go**
([pawnstar-go](https://github.com/jonny-reckless/pawnstar-go), `../pawnstar-go`) and **Rust**
([pawnstar-rs](https://github.com/jonny-reckless/pawnstar-rs), `../pawnstar-rs`). All three engines
produce the identical single-threaded `bench` signature (6,888,983 nodes at depth 13 with net v12)
and identical `PAWNSTAR_BENCH_DEBUG` transcripts (per-position nodes, best moves, every DEBUGX
counter) — that transcript diff is the cross-engine parity check, and this repo's determinism
engineering (the fixed-seed xorshift* Zobrist draw, the stable `SortMoves` insertion sort, the
integer-only eval path) exists precisely to make it possible. **Any search/eval/ordering change
here breaks parity with both ports until re-ported** (the Rust repo's bench-golden test fails by
design; its CLAUDE.md documents the re-bless procedure). A 900-game round-robin at 10+0.1 (2026-07,
300 games/pairing, UHO openings, single-threaded) measured all three within error bars of one
another — C++ vs Rust was an exact 150–150 tie.

## Releases

Releases are cut manually from [`.github/workflows/release.yml`](.github/workflows/release.yml), a `workflow_dispatch`-only workflow (so only write-access maintainers can trigger it; `gh workflow run Release`). It **promotes an existing successful CI run** rather than rebuilding: inputs are `run_id` (blank ⇒ latest successful `CI` run on `main`) and `draft` (default `true`). The job validates the run is a green `CI` run, `gh run download`s the per-OS binaries that run built and tested, checks out that exact commit, regenerates the Doxygen HTML (`make doc`) and a `git archive` source tarball from it, and `gh release create`s a release tagged `v<major>.<minor>.<build>` at the run's commit (no-clobber: fails if the tag exists). Assets: the three platform binaries (renamed `pawnstar-<ver>-<os>` to avoid the bare-name collision between Linux and macOS), `pawnstar-<ver>-docs-html.zip`, and `pawnstar-<ver>-source.tar.gz` (plus GitHub's automatic source archives). Needs `contents: write` + `actions: read`. [.gitattributes](.gitattributes) marks `.claude/ export-ignore` so the source tarball omits agent/harness state (GitHub's automatic source archives don't honour export-ignore, so only the explicit tarball is guaranteed clean). Promote within the 90-day artifact-retention window.

## Code Formatting

clang-format with Microsoft base style, 120-column limit. Config is in `.clang-format`. The CI `format` job enforces that the **entire tree** stays clang-format-clean (not just edited files), so run clang-format before pushing.

## Architecture

Pawnstar is a UCI chess engine using bitboard board representation and parallel alpha-beta search.

### Board representation

`Position` ([position.h](src/position.h)) is the central state class. It stores:
- A public `pieces_[7]` array of per-piece-type bitboards (indexed by `Piece`; index 0 / `kNone` holds the occupied-squares set) and a public `colors_[2]` array of per-color bitboards.
- A `squares_[64]` array for O(1) square→piece lookup.
- Zobrist hash, castling rights (`CastlingRights`), en passant square, half-move clock, and a checkers bitboard (squares giving check to the side to move).
- A public `bool is_null_move_` (null-move marker) and a public `Color color_to_move_` (side to move), the latter declared last so it occupies what was otherwise a trailing padding byte (`sizeof(Position) == 160`).

`Position` is immutable after construction. `MakeMove` and `MakeNullMove` return a new `Position` by value. `GenerateLegalMoves` / `GenerateLegalCaptures` dispatch to the templated `GenMoves<Color, bool>()`, which uses `Pins` to filter pseudo-legal moves. `pieces_`/`colors_`/`is_null_move_`/`color_to_move_` are public data members read directly (no getters); UCI does not declare game results, so there are no `IsGameOver`/`IsCheckmate`/`IsStalemate` helpers.

`Bitboard` ([bitboard.h](src/bitboard.h)) wraps a `uint64_t` with bitwise operations and a range-based iterator over set bits (`Square` values).

### Move encoding

`Move` ([move.h](src/move.h)) packs all move information into 64 bits:
- Bits 0–5: to square; 6–11: from square; 12–14: moving piece; 15–17: captured piece; 18–21: move type; 22: is-checking flag.
- Bits 23–24: TT node type; 25–32: TT depth (int8_t); 33–40: TT age (uint8_t); 41–63: score (signed 23-bit).

The score field (bits 41–63) is shared: during move ordering it holds the sort score, and in a stored TT entry it holds the search score. The two never coexist — a move being ordered in a move list is never simultaneously a TT entry. The TT metadata bits (23–63) let a transposition entry be just `{hash, move}` (128 bits); see the transposition table section. `Move::WithTTData`/`SetTTAge` pack the metadata and `Bits`/`FromBits` round-trip the raw 64-bit value for the lockless TT.

Move types: `kNonCapture`, `kCapture`, `kPawnDoublePush`, `kEpCapture`, `kCastling`, and promotion variants `kPromotionKnight/Bishop/Rook/Queen`.

`MoveList` is a `StackList`: a fixed-capacity, stack-allocated container, so move generation does no heap allocation. (The search's principal-variation lists (`Variation`) and the per-thread position stack are `std::vector`s — the position stack is reserved up front in the `SearchState` constructor; see Per-thread state.)

### Attack generation

`Attacks` ([attacks.h](src/attacks.h)) uses **magic bitboards** for sliding-piece attacks: for each square the relevant occupancy is masked, multiplied by a precomputed magic and shifted down to a hash slot (`hash = (occupancy & mask) * magic >> shift`). This is a plain 64-bit multiply — portable and fast on every x86-64 CPU, including AMD Zen 1/2 where the BMI2 `pext` instruction is microcoded/slow (pext was the previous implementation; replaced for portability). As with the former pext tables, the hash does **not** index the attack sets directly: it indexes `MagicBitboard::indices_` (one `uint8_t` per hash slot), which indexes `MagicBitboard::attacks_` (the de-duplicated 8-byte attack sets). Many occupancies share an attack set (a rook square has 4096 occupancies but ~100 distinct attack sets), so the 1-byte indirection shrinks the tables ~5× — **~155 KB** total vs ~841 KB for a direct layout (<256 distinct sets per square, so a `uint8_t` index fits). The magic multipliers are baked-in constants (`kBishopMagicNumbers` / `kRookMagicNumbers` in `generated_data.h`, found offline by `tools/dump_magics` via a fixed-seed randomised search); only the `attacks_`/`indices_` tables are filled at program startup from those magics — no per-launch magic search. Regenerate the constants with `./build/dump_magics` only if the occupancy-mask or attack generators change (the `assert` in `BuildMagicTable` fires on a stale magic). All other lookup tables live in `src/generated_data.h` as `inline const` globals computed once at startup (no build-time codegen).

Knight, king, and pawn attack tables are plain 64-entry arrays in `generated_data.h`.

`Pins` ([pins.h](src/pins.h)) computes absolute pins and allowed move masks before move generation by scanning enemy sliding pieces that see the king through exactly one friendly piece.

### Search

The search functions were consolidated onto the classes that own their state: `Game::SearchRootNode` is declared in `class Game` and defined inline at the **bottom of [game.h](src/game.h)** — after the `#include "search_state.h"` that completes `SearchState` (see the Build section) — and `Search`/`SearchSingleMove`/`SearchQuiescent`/`IterativeDeepen` are `SearchState` members defined inline in [search_state.h](src/search_state.h). The old standalone `search.h`, `search_alphabeta.cpp`, `search_quiescent.cpp` and `search_root_node.cpp` no longer exist.

**Iterative deepening** — `Game::SearchRootNode` (defined at the bottom of [game.h](src/game.h)) drives iterative deepening from `kStartDepth` (3) upwards, maintaining a principal variation. The per-iteration loop is `SearchState::IterativeDeepen`, which every Lazy SMP thread runs. It runs on a dedicated worker thread started by `Game::StartThinking`; the UCI `stop` command sets `game.is_cancel_pending_`. Time management lives in `ChessClock` ([chess_clock.h](src/chess_clock.h)) — a `std::chrono` clock on the monotonic `steady_clock`: per-move soft/hard budgets are `Duration`s and the hard stop is an absolute `TimePoint` deadline (`HasReachedHardDeadline`, polled every ~2k nodes in `Search`); the soft budget bounds how many iterations the main thread starts. `SearchRootNode` arms it with `StartSearch`/`StartPonderSearch` (a non-positive hard budget = no deadline, i.e. fixed-depth/infinite/pondering).

**Alpha-beta** — `SearchState::Search` ([search_state.h](src/search_state.h)) is a fail-soft negamax with:
- Transposition table probe/store (exact PV, lower-bound CUT, upper-bound ALL).
- Internal iterative reduction (IIR): at a node with no usable TT move and `depth >= kNoTTReduceMinDepth` (4), reduce `depth` by 1. Without a TT move the move ordering is a guess, so the full-depth search is wasteful; the reduced search still fills the TT with a best move, recouped by better ordering on revisit. SPRT-tested **+31.8 ± 16.2 Elo at 8+0.08** (H1, 898 games). Applied after the TT cutoff and before the qsearch drop, so qsearch is unaffected (`depth` stays `>= kNoTTReduceMinDepth-1`).
- Principal variation search (PVS): full window on the first move, null-window scout on the rest, re-search on a fail-high.
- Reverse futility pruning (a.k.a. static null move): at a non-PV node that is not in check, with `depth <= kRfpMaxDepth` (7) and outside the mate zone, if `static_eval - kRfpMargin (80) * depth >= beta` the node is cut and `static_eval` returned. The static eval is computed **once** per node (in `Search`) and reused by both RFP and null-move pruning. SPRT-tested **+114.8 ± 22.5 Elo at 8+0.08** — the single biggest search win to date (pawnstar previously lacked the static-eval pruning suite). Costs a little shallow-depth tactical sharpness (Bratko-Kopec 20/24 at depth 8, recovering to 24/24 by deeper search) but the depth it buys at fixed time wins decisively.
- Null-move pruning.
- Late-move reduction (LMR): reduces any late move (after the 4th) outside a check sequence at depth > 2, with an extra reduction after the 7th (depth > 3), and a third reduction past the 12th move (a pure lateness ladder since the butterfly history table, 2026-07 — pooled counts are rarely zero, so the old zero-history gate lost meaning; that original gate SPRT-tested ~neutral at fast 8+0.08, +11 Elo at 40 moves/min). (Each thread searches its tree sequentially — there is no in-tree split.)
- Late-move (move-count) pruning (LMP): at a non-PV node not in check with `depth <= kLmpMaxDepth` (8), once `move_index >= kLmpBase (3) + depth*depth`, the remaining **quiet, non-checking** moves are skipped entirely (captures/promotions/checks are never pruned). Where LMR *reduces* late quiets, LMP *drops* them — a big effective-branching-factor cut at the frontier. SPRT-tested **+18.6 ± 9.0 Elo at 8+0.08** on top of RFP. Like RFP it costs a little shallow-depth tactical sharpness that deeper search recovers (the Bratko-Kopec accepted sets were regenerated for it).
- Killer move heuristic (2 killers per ply): a quiet move that causes a beta cutoff is stored via `SearchState::RecordKiller`.
- History heuristic (`HistoryTable`) for move ordering — **per-thread** (owned by `SearchState`), so there is no cross-thread contention on its counters.
- Countermove heuristic + 1-ply continuation history (both **per-thread**, owned by `SearchState`, keyed by the previous move's (piece, to-square)): the countermove is the best quiet refutation to the previous move; the continuation history grades how often a quiet move was good as a follow-up to the previous move. The previous move is threaded into `Search`/`ScoreAndSortMoves` as a `prev_move` parameter (Move::None at the root and after a null move).

**Move ordering** — `SearchState::ScoreAndSortMoves` assigns each move a 23-bit ordering score stored in the `Move` itself, in descending bands: winning/equal captures and promotions (`kWinningCaptureBase` + SEE score), the two killer moves for the ply (just below winning captures), the countermove (just below the killers), quiet moves (main history count — a butterfly table [side to move][from/to] pooling statistics across plies (2026-07; was per-ply) — + 1-ply continuation history, clamped below the countermove), and finally losing captures (their negative SEE, sorting below quiet moves). The TT move is searched first, before the list is generated and sorted.

**Quiescence search** — `SearchState::SearchQuiescent` ([search_state.h](src/search_state.h)) searches captures only (no transposition table — it does not probe or store). It applies SEE pruning: because the move list is SEE-sorted by `ScoreAndSortMoves`, the loop reads each move's precomputed ordering score and **exits on the first losing capture** (`move.score() < 0`) — every remaining move is also a losing capture, so none are searched (no per-move SEE recompute). Removing the q-table and switching to this sorted early-exit was SPRT-tested **+38.65 ± 13.78 Elo at 8+0.08** (1318 games, LLR-accepted [-5, 0]).

**Search experiments tried and REJECTED/shelved (don't repeat without a new idea).** Once RFP + LMP were in, the static-eval pruning suite is **saturated** — further pruning/reduction tweaks gave nothing:
- **Futility pruning** (skip quiet moves at frontier depth when `static_eval + margin ≤ alpha`): **−16.5 ± 9.3 Elo at 8+0.08** (H0). RFP + LMP already cover this space; a third static-eval pruner just over-prunes.
- **Log-based LMR reduction table** (replace the fixed 1/2/3-ladder with `R = base + ln(depth)·ln(moveCount)/div`): aggressive (0.75/2.25) **−16** at 8+0.08; gentle (0.50/3.00) **~0 (neutral)**. A pure table swap doesn't beat the tuned ladder; beating it needs the full modulation suite. Branch `lmr-table`.
- **"Improving" heuristic** (is `static_eval` > 2 plies ago? → modulate RFP/LMP/LMR): gentle (±1 ply) **~0 at 8+0.08**; stronger (RFP −50cp, LMP ×⅔ when not improving) **−8 at 8+0.08 and −50 at 40/20** — over-prunes, and worse with depth. Branch `improving` (keeps the per-ply static-eval stack infra). Conclusion: improving doesn't help this engine's already-saturated pruning.
- **Staged move generation — shelved, not worth it (not SPRT'd; rejected on analysis).** Two reasons: (1) the search is NNUE-eval-bound — movegen is only ~1–2% of a node's cost, so even eliminating it is negligible; (2) `GenerateLegalMoves` is a *fast fully-legal* generator (computes checkers + pin masks and filters to the legal set up front), and staging only pays off with pseudo-legal generation + legality-on-make. Retrofitting stages onto the legal generator means either re-running the pin/legality work per stage (paying it repeatedly) or generating everything anyway (saving nothing). Spend movegen-area effort on node-count reduction (move ordering) instead.
- **Correction history (pawn-keyed) — REJECTED, neutral (H0).** A per-colour 16384-entry table keyed by a hash of the pawn bitboards, holding a depth-weighted EMA of (search score − raw static eval); the static eval fed to RFP/null-move was corrected by it, updated at the three non-PV resolution points (gated on quiet move + one-sided bound direction). Binary-vs-binary vs the IIR baseline (same v10 net, TC 8+0.08): **H0 accepted, −3.26 ± 7.80 Elo over 3726 games**. Clean implementation; it just doesn't help an already SPRT-tuned static-eval suite. Reverted. (A tuned/multi-table version — material + non-pawn — might differ, but the basic single table is a no-go.)
- **Singular extensions — REJECTED, time-control-dependent but never a win.** Excluded-move verification search (reduced depth `(depth−1)/2`, window `tt_score − 2·depth`) extending the TT move +1 ply when singular; gated to depth ≥ 8, a cut/PV TT entry. Confirmed firing (243–747/position at d12). Binary-vs-binary vs the IIR baseline, same v10 net: **8+0.08 → H0, −16.96 ± 13.60 (1230 games)**; retested at **40/20 → hovered ~−3 to −5, drifting to H0**. The sign improved with slower TC (−17 → ~−4) but it never became a win — likely over-extending (margin too small) and/or the verification overhead not paying at these TCs. Reverted. If revisited it needs real margin/depth tuning and probably longer TC.
- **Lazy material eval short-circuit — REJECTED, big loss.** `EvaluatePosition` took the caller's `(alpha, beta)` window; if a cheap material-only score (SEE piece values, rule50-scaled) was outside the window by a margin it returned material and skipped the NNUE forward pass *and* the accumulator catch-up. **The speedup is real** — at margin 300 it fired on **59% of eval calls** (forward passes −70%, +38% nps, ~21% faster to a fixed bench depth) — but quality collapsed: Bratko-Kopec **24→22/24 at d8, *worsening* to 21/24 at d12** (not the recoverable shallow-depth sharpness RFP/LMP cost), and head-to-head vs baseline (same v12 net, TC 10+0.1) **H0 accepted at 256 games, −118.4 ± 28.2 Elo (33.6%)**. A wider 600cp margin still failed BK 22/24 while cutting the firing rate to 27% (speedup mostly gone). Root cause: **NNUE diverges from raw material by ≫ any margin that still fires often enough to help** (positional pressure, king safety, compensation for a sacrifice), so the lazy path returns evals that are simply wrong and wreck RFP/null-move/qsearch move selection. The "cheap proxy" would need to *be* the net (e.g. an FT-only estimate), which isn't cheap. Don't repeat with raw material. Reverted.

The headroom is on **move-ordering quality** (history/SEE refinements) and the **eval** (more data, finer bucket maps). Search-speed wins this round: **IIR shipped (+31.8)**; pruning/reduction tweaks remain saturated.

**Parallel search (Lazy SMP)** — `Game::SearchRootNode` spawns `Game::thread_count_` threads (clamped to `kMaxSearchThreads`, 256), each running a complete independent `SearchState::IterativeDeepen` search of the root. There is no tree splitting and no work queue — threads cooperate only through the shared lockless transposition table, where one thread's entries prefill another's probes. Each thread has its own `SearchState` (including its own history table), so the only shared mutable state is the transposition tables and `is_cancel_pending_`. The thread count is `Game::thread_count_`, set by the UCI `Threads` option; its default is computed once at construction (`Game::ComputeDefaultThreads`): the `PAWNSTAR_THREADS` environment variable if set (>0), else `hardware_concurrency()`, clamped to `[1, kMaxSearchThreads]` (`PAWNSTAR_THREADS=1` gives a deterministic single-threaded search — used to regenerate BK reference data). To stop helpers recomputing the same tree in lockstep, the main thread (thread 0) searches every depth while each helper follows a Stockfish-style iteration-skip schedule (`kSkipSize`/`kSkipPhase` tables in `search_state.h`) that skips selected depths. Only the main thread prints `info`, applies time management and returns the authoritative move; because helpers race ahead through the table, the result (move and exact score) is non-deterministic between runs.

### Per-thread state

`SearchState` ([search_state.h](src/search_state.h)) owns:
- A reference to the shared `Game` (TT, clock, cancellation).
- `history_` — the **per-thread** `HistoryTable` (no cross-thread sharing).
- `killers_` (a `KillerArray` = `std::array<std::array<Move, 2>, kMaxPly>`) — killer moves.
- `countermoves_` and `continuation_history_` — the **per-thread** countermove table and 1-ply continuation-history table (keyed by the previous move's (piece, to-square); `continuation_history_` is heap-allocated, ~0.4 MB).
- `node_count_` — nodes searched by this thread.
- `positions_` (`std::vector<Position>`, reserved to `kMaxPly + 4` in the constructor) — per-thread copy-make position stack for the search tree. Reserved once up front so no `push_back` during search reallocates (which would invalidate the `CurrentPosition()` reference and break incremental NNUE updates).
- `accumulator_` (`nnue::Accumulator`) plus `accumulator_ply_` — the NNUE accumulator for the tip position, maintained incrementally but **lazily**: `PlayMove`/`MakeNullMove` do *not* advance it; `SearchState::CurrentAccumulator()` catches it up (one ply at a time, tracked by `accumulator_ply_`) only when an evaluation actually reads it, and only on an eval-cache **miss**, so nodes that cut off before evaluating (TT cutoffs, RFP/LMP/null-move returns) pay zero accumulator cost. `UndoMove` reverses a ply only if the catch-up had reached it. Bit-identical to eager maintenance (same deltas, same order; `test_nnue_incremental` gate) — SPRT-measured **+20.66 ± 9.04 Elo at 8+0.08** (+13.6% nps). A net is always loaded wherever a `SearchState` exists (the engine loads one at startup; the NNUE tests load one before searching; perft/SEE/book tests never build a `Game`), so the accumulator is maintained unconditionally.
- `hash_stack_` (`std::vector<HashEntry>`, reserved in the constructor to game length + `kMaxPly` + 4) — Zobrist hash history for 50-move/repetition detection. One reservation per search, so the per-node hot path still allocates nothing.

Lazy SMP retired the old YBW machinery: `SearchStatePool` and `ThreadPool` (and their `kSearchStatePoolCapacity`/`kThreadPoolQueueCapacity` constants) no longer exist. Threads are plain `std::thread`s spawned per search in `SearchRootNode`, each constructing its own `SearchState` on its stack.

### Transposition table

One table: 64 MB main by default (`kHashtableMegabytes`), resizable at runtime via the UCI `Hash` option (`TranspositionTable::Resize`, which reallocates and clears the table). (Quiescence search has no table of its own.) Each entry is exactly 128 bits: a 64-bit Zobrist `hash` and a 64-bit `move` whose spare bits encode the score, depth, node type (CUT/ALL/PV) and age. `Transposition` exposes `score()`/`depth()`/`node_type()`/`age()` accessors that read those packed bits.

The table is **lockless** (Hyatt's XOR trick). Each cell (`AtomicEntry`) stores two `std::atomic<uint64_t>`: `key = hash ^ data` and `data` (the packed move). A reader accepts a cell only when `key ^ data == probe hash`, so a torn pair written by concurrent stores is detected and read as a miss. Words are accessed with `relaxed` ordering; the XOR check supplies the cross-word consistency that relaxed ordering does not. Writers race benignly (last-writer-wins) — no spinlocks.

`TranspositionTable::Prefetch(hash)` issues a `__builtin_prefetch` for the cell a future probe will read; `SearchSingleMove` calls it right after `PlayMove` so the (random-access) line is resident by the time the recursive node probes the table. Result-neutral; pure latency hiding.

Aging is O(1): the table holds a `generation_` counter that `Age()` increments before each search; an entry is stale when its `age != generation_`, and `RecordTransposition` stamps the current generation when it writes. Replacement policy: prefer replacing stale entries, lower-depth entries, or any entry for PV nodes.

### Evaluation

`EvaluatePosition` (declared in [evaluation.h](src/evaluation.h), defined inline in the [search_state.h](src/search_state.h) hub) is **NNUE-only**: after the draw short-circuit (`kDrawScore` for material/fifty-move/repetition draws) it returns the network's score for the (lazily-maintained) accumulator, **clamped to ±`kMaxEvaluation` (30000)** in `Dequant` so a pathological max-material position can't overflow the int16 eval cache or collide with the mate band (the score band is `|eval| ≤ 30000 < kWinThreshold 31000 < |mate| < kBeta 32000 < int16 max`), memoised by Zobrist hash in the net-specific eval cache, then **scaled toward draw by the fifty-move clock** (`ScaleByRule50`: identity for `reversible_move_count_ <= 20` plies, then a linear ramp to 0 as the clock approaches 100). NNUE inputs are piece placements only, so without this a winning eval is identical at clock 0 and 99 and the search has no gradient to prefer progress (a clock-resetting pawn move/capture) over shuffling in a won-but-unconverted position. The scaling is applied **after** the eval cache (the cache stores the raw, clock-independent eval; the Zobrist hash excludes the clock). SPRT-measured **Elo-neutral at 8+0.08** (−0.25 ± 3.08, 25k games) — kept as a non-regressing correctness fix (it stops the engine drawing won endgames by the fifty-move rule; the gain is in longer TC / vs weaker opponents, which equal-strength fast self-play can't see). It does **not** help cases with no clock-resetting move available (e.g. KBN-vs-K, where every move raises the clock so the scaling has no gradient — that needs mating technique/depth, a separate matter). NNUE is the engine's only evaluator — a loaded net is required to search; `main.cpp` loads the shipped net at startup, falls back to a copy embedded in the binary if the file can't be loaded, and exits only if both fail. The former handcrafted evaluation (material, piece-square tables, pawn structure, king safety, mobility, tapering) and its `EvaluateMaterial`/`DeterminePawnStructure`/etc. functions, the PST/mask tables in `evaluation.h`, and the HCE-only generated bitboards (`kKingAttacks2`, `kKingPawnShelter*`, the pawn-structure masks) were all removed when NNUE became the sole evaluator. `evaluation.h` now declares only `EvaluatePosition`.

SEE (static exchange evaluation, [static_exchange_evaluation.h](src/static_exchange_evaluation.h)) resolves a capture sequence on a square; `EvaluateStaticExchange` returns `{score, is_checking}`. It computes **without a full `Position::MakeMove`** — it applies just the capturing move to a lightweight `SeeBoard` (the post-move piece/colour bitboards) and derives the check flag directly via `SeeAttackersTo` (a `SeeBoard` mirror of `Position::AttacksTo`: a move gives check iff the side that just moved now attacks the opposing king — covering direct, discovered, promotion and en-passant-discovered checks uniformly). This is bit-identical to the kept `EvaluateStaticExchangeReference` (the old MakeMove path), asserted by `test_see`'s cross-check over a recursive move tree, and ~+0.9% nps. It is independent of the NNUE eval: it orders captures and promotions in `ScoreAndSortMoves` (see Move ordering above), prunes losing captures in quiescence search, and is exercised by `test_see`.

### NNUE evaluation

`nnue::Network` ([nnue.h](src/nnue.h)) is the engine's learned evaluation — the only evaluator. The `Game` owns one `nnue::Network` instance (the public member `nnue_network_`); there are no nnue globals. A net is **always** loaded in normal operation (the engine loads the shipped net, or its embedded fallback, at startup and exits only if both fail), so the accumulator is maintained unconditionally and there is no "is NNUE active" toggle (a relic of the removed HCE — deleted). The public `Network::loaded_` flag reports whether a net is present, read by the `eval`/`nnue` diagnostics. After the draw short-circuit `EvaluatePosition` evaluates with the net.

- **Architecture**: a perspective net with bullet's `ChessBuckets` feature set — `768` inputs per (perspective, king bucket) → `1024` feature transformer (shared across both perspectives), SCReLU activation, `concat[stm | ntm] = 2048 → 1` output (centipawns, side-to-move relative). The shipped net (**v12**) uses **8 king buckets** selected by king file-pair × board-half (`kKingBucketMap`). Constants: `kHiddenSize=1024`, `kNumKingBuckets=8`, `kFeatureRows=768*8`, `kQA=255`, `kQB=64`, `kScale=400`.
- **int8 output layer (shipped)**: the engine's eval (`Network::Evaluate`) runs the output dot in **int8** — uint8 SCReLU activations (`clamp(x,0,QA)² >> kInt8Shift`, `kInt8Shift=9` → `[0,127]`) × int8 output weights, accumulated in int32. ~1.86× faster than the exact int16 dot (a measured **+31.8 ± 10.3 Elo at 8+0.08**), at a bounded ~26 cp deviation. `S=9` keeps every pair within int16 so the AVX2 (`pmaddubsw`/`maddubs`) and AVX-VNNI (`vpdpbusd`) kernels are saturation-free and bit-identical. **The kernel is chosen at runtime by cpuid** (`kCpuHasAvxVnni` = `CPUID.7.1:EAX` bit 4 → `OutputDotInt8Vnni` else `OutputDotInt8Avx2`, ~+1.3% nps on VNNI CPUs). The VNNI kernel is compiled behind a function-level `[[gnu::target("avx2,avxvnni")]]` attribute, so `vpdpbusd` is emitted only inside it and the engine stays a single baseline `-mavx2` binary that **also loads and runs on AVX2-only CPUs** (the gate never calls the VNNI kernel there). The exact int16 SCReLU forward pass remains as `Network::EvaluateExact` — the reference that `test_nnue` cross-checks against bullet (±2 cp) and that the int8 path approximates. (The feature transformer stays int16 — the int8 *feature*-weight experiment was measured and **removed** after a definitive −8 Elo SPRT; see [nnue/int8_quant_study.md](nnue/int8_quant_study.md).)
- **King buckets**: the shipped net (v12) uses **8 banks** selected by king **file-pair × board-half** (`kKingBucketMap` = `(file/2) + 4·(king in its own advanced half)`: files a/b→0, c/d→1, e/f→2, g/h→3 for a king on ranks 1–4, and the same +4 (banks 4–7) for a king advanced onto ranks 5–8, per-perspective oriented — file-pair captures king safety and the castled-king pawn shelter, and the own-half/advanced rank split captures a real king-safety signal; back-rank kings exercise the lower four banks and an advanced king the upper four). Each perspective selects its bank by *its own* king's square (in that perspective's orientation) via the 64-entry `kKingBucketMap` (ranks 1–4 → `0,0,1,1,2,2,3,3`; ranks 5–8 → `4,4,5,5,6,6,7,7`), kept byte-identical to the `ChessBuckets::new` array in `tools/bullet/pawnstar.rs`/`pawnstar_eval.rs`.
- **Feature index** (per perspective): `bucket*768 + colour*384 + (piece_type-1)*64 + square`, with the not-to-move perspective swapping the colour offset and flipping the square with `kRankFlip`. This matches bullet's `ChessBuckets::map_features`.
- **Accumulator** (`nnue::Accumulator`, two perspectives): maintained **incrementally and lazily** on `SearchState` — the update is deferred out of `PlayMove` and applied by `CurrentAccumulator()` only when an eval needs it (see Per-thread state). The *delta* computation is `Network::Update`, whose common case (no king-bucket crossing) finds the changed squares straight from the parent/child **colour bitboards** — a move touches only 2–4 squares, with captures/EP/castling/promotion all handled uniformly — and applies just those feature-column deltas to both perspectives (no per-piece-type scan). A king move that **crosses a bucket boundary** changes that whole perspective's bank, so `Update` rebuilds that one perspective from scratch (`RefreshSide`) and diffs the other (`DiffSide`) — at most one king moves per ply, so at most one side refreshes (with the shipped 8-bucket net, a king move across a file-pair boundary or across the board-half rank boundary triggers this). `Network::Refresh` rebuilds both at the root (seeded with `feature_bias`). The colour-XOR delta is bit-identical to a per-piece diff (same columns; int16 accumulator add/sub is order-independent), and `test_nnue_incremental` checks the incremental accumulator stays bit-identical to a full refresh (random trees, including bucket crossings on multi-bank nets).
- **Weights** are bullet's native quantised payload (tightly packed little-endian `int16`: `feature_weights[768*8*1024]` bucket-major, `feature_bias[1024]`, `output_weights[2048]`, scalar `output_bias`; bullet pads the trailing tensor to 64 bytes). `Load` also requantises the output weights to int8 (`output_weights_i8_`, scale `output_w_scale_` = 1 for the shipped net (v12) since `|w| ≤ 127`) for the shipped int8 forward pass. The exact int16 forward pass and dequantisation mirror bullet's example; correctness is verified by `test_nnue` (`EvaluateExact` vs bullet, int8 `Evaluate` vs `EvaluateExact`).
- **Self-describing header**: every loadable net is prepended with a 32-byte `NetHeader` (magic `PSN1` + the arch params `kInputSize`/`kNumKingBuckets`/`kHiddenSize`/`kQA`/`kQB`/`kScale`; see `src/nnue.h`). `Network::Load` **requires the header**: it rejects any net without the `PSN1` magic (a raw/unstamped bullet net) and validates a stamped file's arch fields against the engine's constants, **rejecting a mismatch** (so an incompatible net can't be silently misread). The old headerless-raw acceptance path was removed — a raw bullet net must be stamped first. Stamp a raw net with `tools/stamp_net` (`make tools`) — it embeds the building engine's constants, so it only stamps a matching net (and only accepts a raw, not already-stamped, net).
- **Eval cache**: an 8 MB lockless `EvalCache` (owned by the `Game`) memoises the net-specific static eval by Zobrist hash (single 64-bit atomic per entry: 48-bit key + 16-bit score), so a cache hit in `EvaluatePosition` skips the forward pass. Cleared on `SetPosition` and on `EvalFile` load (evals are net-specific). ~42% hit rate / ~5% faster.
- **Loading**: NNUE is the only evaluator, so a net is **required** — `src/main.cpp` loads `nnue/pawnstar-v12.bin` at startup, resolving the path through `LocateResource` (tries the path relative to the cwd first, then relative to the executable's directory and its parent — so `build/pawnstar` finds the repo-root resources when launched from anywhere), as it does for the book. If the file can't be loaded (absent, wrong cwd, wrong architecture) it **falls back to a copy of the net embedded in the binary** (`src/embedded_net.S` `.incbin`'s the net at the build's `EMBED_NET` path — the CMake build passes it via `-DEMBED_NET_PATH`, the single source of truth, so the embedded copy can't drift from the shipped net; the `.S` embeds into ELF `.rodata` or COFF `.rdata` so this works on Linux and Windows alike, making the engine binary self-contained on both; linked into the engine only), so the engine always has a working evaluator; it exits only if **both** the file and the embedded fallback fail (a corrupt build). Override the path via env (`PAWNSTAR_EVALFILE=<path>`) or load a different net at runtime via UCI `setoption name EvalFile value <path>`. The embedded fallback is loaded via `Network::LoadFromMemory`, which shares the exact header detection/validation of `Network::Load`. There is no `UseNNUE` option (nothing to toggle to). The network is a single `nnue::Network` owned by the `Game`, read-only after load and shared by all Lazy SMP threads, which call only its `const` methods. The `Game` (and hence the `Network`) is **heap-allocated** (`std::make_unique<Game>()` in `main.cpp` and the tests): at 8 king buckets the inline ~12.6 MB `feature_weights_` array would overflow the default 8 MB stack if the owning object were a local. The FT weights stay an **inline member** of the heap `Game` rather than a `unique_ptr` to a separate weight array — a separate allocation is page-aligned, so the 2048-byte feature-column stride collides in the same cache sets and costs ~10% nps (measured); keeping the array inline in the heap object avoids the cache aliasing and is speed-neutral.

**Training** (see [nnue/README.md](nnue/README.md) and [tools/](tools)): the shipped nets are trained on **public PlentyChess** bulletformat data (strong-engine labels — label quality, not self-play volume, was the lever) via the bullet trainer (`tools/bullet/pawnstar.rs`, pinned commit installed by `tools/setup_bullet.sh`). (Self-play data generation — `gen_data.cpp` / `run_gendata.sh` / `make_openings.sh` — was the original bootstrap source and has been removed.) Minimal flow: `tools/setup_bullet.sh` → `OPENINGS=<openings.epd> tools/run_experiment.sh <shuffled.data> <net.bin>` (trains via `tools/train_pipeline.sh`, verifies via `tools/verify_net.sh`, then SPRTs via `tools/run_sprt.sh`). GPU training needs a CUDA toolkit and a driver supporting CUDA ≥ 12.3 (bullet uses `cuFuncLoad`); set `BULLET_FEATURES=""` to train on CPU. NNUE strength is measured by game play (SPRT, now always net-vs-net since the HCE baseline is gone), not by the Bratko-Kopec suite.

`main` ships one net ([nnue/README.md](nnue/README.md) §7): `nnue/pawnstar-v12.bin`, a **1024-wide net with 8 king buckets (file-pair × board-half)** trained on **~6B** public PlentyChess positions — **the engine's only evaluator** (the handcrafted eval was removed). Lineage (all SPRT-measured): self-play labels lost to HCE (label quality was the lever); ~12× more data gave nothing (the 256 net was capacity-limited); doubling width 256→512 added +55 Elo fixed-depth / +71 time-control (**v4**, Chess768); doubling again 512→1024 on 60M gave **~0 fixed-depth and −74 on the clock** (rejected then — 60M saturates 512); **king buckets on 60M were flat (data-starved), but on ~818M gave +47 fixed-depth and +17 on the clock (v6)**, plus two speed fixes (the eval cache and a single-pass `Update`); **scaling that 512×4 arch to ~2.31B gave +29.96 fixed-depth and +20.73 on the clock (v7)**; **then, at that same 2.31B, doubling width to 1024 *without* king buckets (a single bank) beat v7 by +31.9 ± 16.6 Elo at 40/20 — and at half the file size — reversing the old 60M result now that data no longer saturates the wider net (v8)**; **then, holding width fixed at 1024 and the data at ~2.25B, adding 4 king buckets (file-pair) beat v8 by +11.4 ± 6.75 Elo at 8+0.08 (v9) — the bucket advantage shrinks with data (it was +29 at 750M) but stays positive at scale, so the old "width beats buckets" call was really width-vs-buckets confounded; at equal width, buckets still help**; **then, holding that v9 arch (1024 + 4 file-pair buckets) fixed and scaling the data 2.31B→3.82B (four more PlentyChess shards), beat v9 by +16.3 ± 10.8 Elo at 8+0.08 (v10) — more data again the single biggest lever**; **then, holding that same arch fixed and scaling the data 3.82B→6.05B (six more PlentyChess shards), beat v10 by +9.28 ± 6.58 Elo at 8+0.08 (v11) — more data again, the gain diminishing as the base grows**; **then, holding that same ~6B pool fixed and doubling the king buckets 4→8 (file-pair × board-half — bucket = `file/2`, +4 when the king is in its own advanced half, ranks 5–8), beat v11 by +17.29 ± 8.68 Elo at 8+0.08 (3520 games) and +10.97 ± 6.57 at 12+0.12 (5610 games) (v12, shipped) — single variable vs v11 = bucket count**. **The recurring lesson: more data is the lever** (v12's ~6B is still only ~29% of the ~21B available) — **but king buckets are *not* at diminishing returns**: the 0→4 step was +11.4 (v9), the 4→8 step is *bigger* (+17/+11), because the rank split captures real own-half-vs-advanced king-safety signal. (Note the data-scaling steps v9→v10→v11 still diminished; adding *more buckets at fixed data* did not.) Incremental + lazy/deferred accumulator updates (the latter +20.66 Elo / +13.6% nps) + AVX2 SIMD + the int8 output layer + the eval cache keep NNUE fast enough to win on the clock, not just at equal depth. Nets of different architecture/width are size-gated and not cross-loadable, so each shipped arch re-ships its net + a matching `test/nnue_reference.txt` (each predecessor was retired as its successor shipped — most recently v11, whose `.bin` is removed from the repo).

### Continuing NNUE development (experiment playbook + state)

Entry point for further NNUE work, including from a fresh/cloud checkout. The architecture and the full
train → verify → SPRT → ship recipe live in [nnue/README.md](nnue/README.md) (§6 step-by-step, §7 shipped
nets + lineage + troubleshooting); this captures the durable *operational* context and experiment state
that isn't otherwise in the repo.

**Current shipped net:** `nnue/pawnstar-v12.bin` — 1024-wide with **8 king buckets (file-pair × board-half)**, ~6B public
PlentyChess positions. Engine arch is `kHiddenSize=1024` / `kNumKingBuckets=8` in [nnue.h](src/nnue.h); the
trainer must match ([tools/bullet/pawnstar.rs](tools/bullet/pawnstar.rs) + `pawnstar_eval.rs`, and the
file-pair × board-half `kKingBucketMap` in [nnue.h](src/nnue.h)). The 12.6 MB net needs the owning `Game`
heap-allocated (`std::make_unique<Game>()`) — at 8 buckets the inline `feature_weights_` array overflows the
8 MB stack otherwise.

**What's been tried (all SPRT-measured — don't repeat):**
- **Width sweep at 2.31B:** 256→512 = +55 depth / +71 tc (v4); **512→1024 = +31.9 @ 40/20 (v8)**;
  **1024→1536 = −17 @ 40/20 (REJECTED)**. So **1024 is the width sweet spot at this data scale** — wider
  costs more per move than its eval gain returns on the clock. On only 60M, 1024 lost −74 on the clock (v5):
  width needs data.
- **King buckets:** flat on 60M (data-starved); +47 depth / +17 clock on 818M (512-wide, v6); at
  **1024-wide they win at every data scale tested holding width fixed — +29 Elo at 750M, +11.4 ± 6.75 at
  ~2.25B for the 0→4 step (v9)**, and **doubling 4→8 (file-pair × board-half) at ~6B beat v11 by +17.29 ±
  8.68 @ 8+0.08 and +10.97 ± 6.57 @ 12+0.12 (v12, shipped)**. The earlier "v8 dropped
  buckets / width beats buckets" call was **width-vs-buckets confounded** (it compared 512+buckets v7 to
  1024-no-buckets v8); at equal width, buckets help. **Crucially, king buckets are NOT at diminishing
  returns** — the 4→8 step (+17/+11) is *bigger* than the 0→4 step (+11.4), because the own-half/advanced
  rank split captures real king-safety signal; this is the *opposite* of the old "bucket advantage shrinks
  with data" expectation (the data-scaling steps v9→v10→v11 still diminished, but adding *more buckets at
  fixed data* did not). **File-pair bucketing (`file/2`) is preferable to
  quadrant/rank-based** — king safety and the castled-king pawn shelter are file-dependent, and back-rank
  kings (where kings spend most of the game) exercise all four file banks, whereas a rank-based quadrant
  scheme leaves the upper banks nearly unused. Cost: 8× the FT rows (net 1.58→12.6 MB, so the `Game` is
  heap-allocated) and a small per-eval hit, already priced into the TC SPRT.
- **More data is the recurring lever** — **v10→v11: scaling 3.82B→6.05B (six more shards, same 1024 + 4-bucket arch) gave +9.28 ± 6.58 Elo at 8+0.08 (LOS 99.7%)**; the gain is diminishing as the base grows (v9→v10 was +16.3 for +1.5B; v10→v11 +9.3 for +2.2B), but v11's 6.05B is still only ~29% of the ~21B PlentyChess pool. (v11→v12 then held the data fixed and won purely on architecture — the 4→8 bucket double; see the king-buckets bullet.)
- **Speed** (lets a wider net win on the clock): incremental accumulator (+80), AVX2 (+180), eval cache
  (~5%; 8→32 MB gave nothing — exhausted), single-pass `Update`, colour-XOR move-driven `Update` (~3% at
  1024), **int8 output layer (+31.8 ± 10.3 at 8+0.08, shipped)** — 1.86× faster output dot. **int8 *feature*
  weights (lossless scale=1 retrain): −8 Elo, rejected** (FT `Update` not memory-bound at 1024×1). Aspiration
  windows: no gain (tried). **Quiescence TT removal + SEE sorted early-exit: +38.65 ± 13.8 at 8+0.08, shipped.**
  **Hand-written inline asm for the forward kernels (output dot + FT add/sub, 4 dot accumulators): −3% nps
  vs clang intrinsics, REJECTED** — bit-identical (bench node count unchanged) but the materialise-then-dot
  buffer overhead beat the multi-accumulator gain; clang's `-O3` codegen already wins. (Gotcha: in inline asm
  `{vex}` must be escaped `%{vex%}`, else clang emits EVEX `vpdpbusd` → SIGILL on non-AVX-512 CPUs.)
- **Head depth — a hidden layer in the head (`concat 2048 → 16 → 1`, SCReLU): REJECTED, −11.3 ± 12.9 Elo at
  fixed depth 8, H0 (1908 games).** Single-variable A/B: a control (current `2048 → 1` head) and the candidate
  trained on the *same* shuffled 200M PlentyChess set + same schedule, then head-to-head SPRT. Fixed depth is
  pure eval quality (speed-independent) and it's already negative there, so no time-control follow-up was run
  (a slower head that doesn't improve the evaluator at equal nodes only loses more on the clock); conservative,
  since the control used its int8 head (~24 cp deviation) vs the candidate's exact float head and still won.
  Kept the FT int16 + incremental and ran the **float** head (f32-saved weights). Branch `nnue-hidden-layer`
  (pushed, not merged). **Caveat:** reused the 2-layer LR schedule/epochs — a deeper head may want tuned
  hyperparameters and/or more data; this rejects "hidden layer with the *same recipe*", not all possible heads.
  **Gotcha:** bullet stores affine weights **input-major** (`w[input*out+output]`); the `O=1` output layer
  hides this, so reading the `2048→16` layer output-major gives a dead near-constant head — and a 0 cp
  `test_nnue` cross-check *passes while broken* (engine and reference misread identically), so **sanity-check
  that evals track material (up-a-queen → ~+1200) before trusting a new arch.**
- **Output buckets — 8 piece-count banks on the v11 head (`concat 2048 → 8`, `select` by `MaterialCount<8>`):
  REJECTED at scale.** Single-variable A/B (control = v11 arch, candidate = v11 + buckets, same pool + recipe).
  Weak **+6.1 ± 9.5 fixed-depth at 500M (H0, inconclusive)**, but at **6B (v11's ~6.36B pool, SBS=300) it
  REGRESSES at both time controls: −22.96 ± 15.71 @ 8+0.08 and −14.70 ± 13.27 @ 12+0.12 (both H0)**. The slower
  TC is less negative (−23 → −15, classic TC-dependence — the 8× output table's per-move speed cost weighs less)
  but never positive. Two causes: **bucket gains shrink with data** (the king-bucket precedent — +29 @ 750M →
  +11 @ 2.31B — applies to output buckets too) and a **small TC speed penalty** (int8 output table 8× larger,
  16 KB vs 2 KB; the 500M test was fixed-depth so never paid it). FT unchanged; output saved bucket-major via
  bullet `.transpose()`, so the int8/int16 kernels are reused on the selected bank (no float path). Branch
  `nnue-output-buckets` (not merged); trainer `tools/bullet/pawnstar_ob.rs`, ref `pawnstar_eval_ob.rs`.

**Open levers / candidate next experiments:**
- **More king buckets / a better bucket map** — v12 ships 8 buckets (file-pair × board-half). The 4→8 double
  was a clear win (+17/+11), so buckets are clearly *not* saturated: untested next steps are still more banks
  (e.g. a finer file+rank map, or mirrored buckets to halve the input space). (File-based beats quadrant/
  rank-based — see the king-buckets note above.)
- **More data** — add PlentyChess shards beyond the ~6B pool (v11/v12 used 16 shards; the dataset has ~168 ≈ 21B).
- **int8 *output* layer — DONE, shipped** (+31.8 Elo at 8+0.08; runs on plain AVX2 via `pmaddubsw`). The
  bit-identical AVX-VNNI `vpdpbusd` kernel is selected at **runtime by cpuid** (~+1.3% nps on capable CPUs),
  compiled behind a function-level target attribute so the single `-mavx2` binary still runs on AVX2-only
  CPUs — no `VNNI=1` build flag. See [nnue/int8_quant_study.md](nnue/int8_quant_study.md).
- **int8 *feature* weights — DONE, REJECTED (−8 Elo), code removed.** Was implemented behind `INT8_FT=1`.
  The quantisation-aware retrain (FT weights clipped to ±127/QA → `scale=1`, lossless, no reconstruct
  `mullo`) was trained and SPRT'd as a pure speed test (bit-identical evals, same net, TC 8+0.08): **H0
  accepted, −8.04 ± 6.85 Elo over 5314 games**. The FT `Update` isn't memory-bound at 1024×1, so halving
  the weight bytes doesn't pay for the int8→int16 widening — even the best case loses. The engine `INT8_FT`
  support, the `INT8_FT=1` build flag, and the trainer FT-weight clipping were all **deleted** afterwards
  (the FT is unconditionally int16 again); the analysis lives in the study doc. *Don't reintroduce it.*
- **More data / king buckets on 1024** remain the eval-quality levers; int8 was the speed lever and the
  output half is now banked.

**Gotchas (each cost a cycle):**
- **Not version-controlled:** the training data, the `bullet` checkout, and baseline engine binaries are
  NOT in the repo — a fresh/cloud env must (re)create them: `tools/setup_bullet.sh` (pinned bullet commit),
  then download + concat + shuffle the PlentyChess shards per nnue/README §7. GPU needs CUDA ≥ 12.3; set
  `BULLET_FEATURES=""` to train on CPU.
- **Cross-arch SPRT:** a new-arch candidate engine's default *and embedded* nets are the old (shipped) arch,
  so it fatal-exits at startup before fastchess can send `setoption EvalFile`. Give it a net at startup:
  `CAND_STARTUP_NET=<cand net> BIN_A=<cand engine> BIN_B=<baseline engine> BASELINE_NET=<old net> TC=40/20 tools/run_sprt.sh <cand net> <openings> <rounds>`
  (run_sprt.sh wraps the candidate when `CAND_STARTUP_NET` is set).
- **`pawnstar_eval` is NOT header-aware** → feed it the **raw** (unstamped) net. But `test_nnue` /
  `test_nnue_incremental` now load through the engine, which **only accepts a stamped net** (the raw path
  was removed), so they must be given a stamped net. `tools/verify_net.sh` handles this: it runs
  `pawnstar_eval` on the raw net and stamps a scratch copy for the engine-side tests. Rebuild
  `pawnstar_eval` when the arch changes.
- **Shipping a new net** must: stamp it (`build/stamp_net`), regenerate `test/nnue_reference.txt` (from the
  raw net) and the Bratko-Kopec accepted moves (`test/bratko_kopec_nnue_test.cpp`, verify 24/24 at depths
  8–11), point `EMBED_NET` / `main.cpp` at it, retire the old net (size+header gated, not cross-loadable),
  and update docs — full checklist in nnue/README §7.
- **Re-embedding a new net needs a rebuild.** `make check` does build the engine, but the shipped net is
  `.incbin`'d at CMake's `EMBED_NET` path, so after you replace the net file or repoint `EMBED_NET`
  (in `CMakeLists.txt`) / `main.cpp` you must rebuild (`make` or `cmake --build build`) to relink the new
  embedded copy into `build/pawnstar` before testing the engine binary or running a same-cwd SPRT.
- **Trainer edits don't take effect until re-synced + rebuilt (cost a full ~3h cycle once).** The repo's
  `tools/bullet/pawnstar.rs` is *copied* into the bullet checkout (`~/pawnstar_nnue/bullet/examples/pawnstar.rs`)
  by `tools/setup_bullet.sh`; training compiles **that copy**. Editing the repo file alone changes nothing.
  After editing, `cp tools/bullet/pawnstar.rs <bullet>/examples/` (or re-run `setup_bullet.sh`), rebuild
  (`cargo build --release --features cuda --example pawnstar`), and confirm the example binary is newer than
  the source. A 750M run was wasted once because a clipped trainer was edited but never synced — the stale
  unclipped binary trained.
- **Spot-check a quantisation-aware retrain at SB1 before sinking full GPU time.** Set `PAWNSTAR_SAVE_RATE=1`
  (env, added to the trainer) to checkpoint every superbatch, then inspect the first `quantised.bin` (~5 min):
  e.g. for FT-weight clipping, `max|FT weight|` must be ≤127 (`scale=1`) — read it straight from the raw net
  with numpy (`np.fromfile(...,dtype=np.int16)[:768*1024]`). The *unclipped* run saturates to `max=505`
  almost immediately, so SB1 is already a conclusive discriminator. Abort in minutes if the clipping/quant
  didn't take, instead of discovering it after the full run.

### Opening book

`OpeningBook` ([opening_book.h](src/opening_book.h)) is loaded from `pawnstar.book` in the working directory at startup (`main.cpp`); the shipped book ([pawnstar.book](pawnstar.book)) is the public-domain [TSCP](https://github.com/terredeciels/TSCP) `book.txt`. As with the NNUE net, if the file can't be loaded (absent, wrong cwd) the engine **falls back to a copy of the book embedded in the binary** (`src/embedded_book.S` `.incbin`'s the book at the build's `EMBED_BOOK` path via `-DEMBED_BOOK_PATH` (the CMake build), ELF or COFF, so the embedded copy can't drift from the shipped book; linked into the engine only) via `OpeningBook::InitializeFromMemory`; the book is optional, so failing both is just a warning. (The built-in book lookup is also gated by the UCI `OwnBook` option.) The format is plain text, one opening line per row, each move in **coordinate notation** (e.g. `e2e4 e7e5 g1f3`); `ParseLineOfPlay` replays each line from the start position, ignoring tokens that begin with a digit (move numbers) and treating `#` as a to-end-of-line comment. The book is a `std::map<zobrist_t, std::vector<Move>>` storing moves *with repeats*, so `GetMove`'s uniform-random pick is frequency-weighted. `SearchRootNode` returns a book move immediately on a hit, before searching; the `bookmoves` UCI command lists moves and frequencies for the current position. There is no SAN/PGN parser — coordinate notation only.

### UCI protocol

[input_handling.h](src/input_handling.h) parses all UCI commands plus engine-specific diagnostics: `eval`, `nnue`, `getboard` (print board), `bookmoves` (list opening-book moves for the current position), `dbg` (dump debug counters), `bench` (search a fixed position set — `kBenchFens`, the Bratko-Kopec positions — to a fixed depth `kBenchDepth` (13, or `bench <depth>`) single-threaded with the book off and hash cleared, printing `<nodes> nodes <nps> nps`; the node total is a determinism signature and the nps a speed-regression check). **`eval` vs `nnue` are *not* duplicates:** `eval` prints `EvaluatePosition` — the **full** value the search uses (draw short-circuit → eval-cache probe → NNUE forward pass → `ScaleByRule50` fifty-move scaling); `nnue` prints `Network::Evaluate(position)` — the **raw** network output with none of that wrapping. They print the same number in a plain position but diverge on a draw (`eval` → 0) or once the fifty-move clock passes 20 plies (`eval` scaled toward 0, `nnue` unscaled). `setoption` handles `EvalFile` (string, path to a net), `Hash` (spin, transposition-table size in MB, 1–4096 → `TranspositionTable::Resize`), `Threads` (spin, Lazy SMP search threads, 1–`kMaxSearchThreads` → `Game::thread_count_`), `Clear Hash` (button → `TranspositionTable::Clear`, empties the table), `Move Overhead` (spin, ms reserved from each hard deadline for GUI/network lag, 0–5000, default 30 → `Game::move_overhead_`) and `OwnBook` (check, default true → `Game::use_own_book_`, gates the built-in opening-book lookup in `SearchRootNode`); these plus `Ponder` (check) are advertised in the `uci` response. **`Ponder` is advertised for discovery but its value is a no-op** — `handle_setoption` ignores it; pondering is purely reactive to `go ponder` (per the UCI spec the option only signals that pondering is *allowed*, e.g. to adjust time management, which Pawnstar does not do). (There is no `UseNNUE` option — NNUE is the only evaluator.)

**`go` time controls** — `handle_go` parses the flags `infinite`/`ponder` and the value keywords `wtime`/`btime`/`winc`/`binc`/`movetime`/`depth`/`movestogo`/`nodes` (only the side-to-move's `wtime`/`btime`/`winc`/`binc` are kept; the opponent's are ignored, as is `searchmoves`). `go nodes <n>` runs with no clock (`kInfinite`) and stops at the node budget (`ChessClock::max_nodes_`, checked per-thread in `Search` alongside the deadline — exact at `Threads=1`, approximate above). Time planning in `SearchRootNode`: the soft budget is `remaining_time / moves_to_go` and the configured `Move Overhead` (default 30 ms) is subtracted from the hard deadline (movetime included; fixed-depth/infinite have no deadline). The hard deadline is polled every ~2k nodes (`& 0x7FF` in `Search`); a coarser ~64k poll let the engine overrun by 6–10 ms and forfeit once it actually spent time near the deadline. **`ChessClock::increment` is parsed (cleared at each `go`, set from `winc`/`binc`) but deliberately NOT folded into the budget**: doing so (`+ increment / 2`) was SPRT-tested and was neutral-to-slightly-negative — **−14.5 ± 21 at 8+0.08 (480 games)** and **−2.5 ± 7.9 at 10+0.1 (3562 games)**, zero forfeits — so it was reverted (it may pay off at much slower increment TCs, untested).

**`info` output** — each time the root best move improves the main thread prints `info depth … score … time … nodes … nps … hashfull … pv …`. The score is `score mate <±moves>` when a forced mate is found (`|score| ≥ kWinThreshold`; positive = we mate, negative = we get mated), else `score cp <centipawns>`. `nps` is nodes·1000/elapsed-ms (this thread's `node_count_`) and `hashfull` is `TranspositionTable::HashfullPermille` (a 1000-cell sample, per mille).

**Pondering** — `go ponder` searches the (already-set-up) position on the opponent's clock: `handle_go` sets `Game::is_pondering_`, and `SearchRootNode` calls `ChessClock::StartPonderSearch` (records the soft/hard budgets but arms **no** hard deadline) so the search never times out, with the soft-time stop in `IterativeDeepen` gated off while pondering. The main thread publishes the predicted reply (PV[1]) to `Game::ponder_move_`, emitted as `bestmove <m> ponder <reply>`. On `ponderhit` the budget starts *from that moment* (budget-from-ponderhit): `handle_ponderhit` calls `ChessClock::OnPonderhit` (reset the search-start instant, arm the hard deadline) and clears `is_pondering_`, so the running search keeps its work (warm TT, current depth) and converts to a timed search. On `stop` (ponder miss) the search is cancelled and the bestmove emitted (the GUI ignores it). The clock's per-search instants (`search_start_time_`, `hard_deadline_`) are `std::atomic<ChessClock::TimePoint>` so `ponderhit` on the UCI thread can retarget the search the workers read every node. If a ponder search ends on its own before `ponderhit`/`stop`, `SearchThreadEntry` waits (UCI forbids `bestmove` mid-ponder).

### Generated data

[generated_data.h](src/generated_data.h) holds the precomputed lookup tables (attack/ray bitboards, magic-bitboard sliding-attack tables, intervening-squares masks, Zobrist hashes) as `inline const` globals. They are computed once at program startup (dynamic initialisation of the `inline const` globals) by the pure helper functions in the same header — there is no separate generator program or build step. To keep the build helpers (which include a `BishopAttacks`/`RookAttacks` that would otherwise collide with `attacks.h` at global scope) out of the global namespace, they and the table definitions live in a named `gendata_detail` namespace, and the public tables are re-exported to global scope with `using`-declarations. Edit `generated_data.h` when the table logic changes. (Historically these were emitted as literals by a standalone `generate_constants.cpp`, since removed; later they were `extern const` globals defined in `generated_data.cpp`, before the header-only conversion folded them inline.)
